{
  "name": "note-app-root",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install": "cd client && npm install"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongodb-memory-server": "^11.0.1",
    "mongoose": "^9.1.6",
    "socket.io": "^4.8.3"
  }
}
